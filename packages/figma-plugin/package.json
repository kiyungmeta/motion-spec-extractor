{
  "name": "@motion-spec/figma-plugin",
  "version": "1.0.0",
  "description": "Figma plugin for motion spec extraction and visualization",
  "scripts": {
    "build": "esbuild src/code.ts --bundle --outfile=dist/code.js --platform=neutral --target=es2020",
    "watch": "esbuild src/code.ts --bundle --outfile=dist/code.js --platform=neutral --target=es2020 --watch",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@motion-spec/shared": "1.0.0"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.98.0",
    "esbuild": "^0.20.0",
    "typescript": "^5.3.0"
  }
}
